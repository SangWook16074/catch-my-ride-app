# TODO — 플러터 스토어판 (2026-09-09 시작)

> 🧑 = 오너만 할 수 있는 액션 / 🤖 = 에이전트 작업
> 기능 명세: [../PRD.md](../PRD.md) · 서버 계약: [../API.md](../API.md) · 실행 규칙: [CLAUDE.md](CLAUDE.md)
> 미니앱(catch-my-ride-appintoss)에서 검증된 요구사항을 이식한다 — 코드 번역이 아니라 규칙 이식.

## 로드맵 — 차별화·유입·수익화 (오너 결정 2026-09-09, PRD §8·명세서 §9)

전략: **유저 기반 먼저, 수익화는 리텐션 확인 후.** 차별화는 미니앱이 구조적으로 못 하는 것에 집중.
단계는 순서대로 — 앞 단계가 유입 전환율을 결정한다.

### 1단계 — 코어 패리티 + 미니앱→앱 설정 이관
- [x] 🤖 실서버 연동 (mock → API.md 계약, Base URL 동일) — 2026-09-09 완료: §8-3 익명 키(기기 생성) 인증, 푸시 제외 전 엔드포인트. 개발용 mock은 `--dart-define=USE_MOCK=true`
- [ ] 🤖 **설정 이관 플로우**: 미니앱 "앱으로 설정 가져가기" → 서버가 일회용 이관 코드 발급 →
      앱 최초 실행에서 코드 입력 → 익명 키 연결로 경로·설정 그대로. 재온보딩 마찰 제거가 목표.
      서버 API 신설 필요(이관 코드 발급/교환) — API.md에 계약 추가 후 진행
- [ ] 🤖 온보딩·라이브 뷰·피드백·다중 경로 등 미니앱 기능 패리티 완성

### 2단계 — 시스템 표면 (킬러 차별화, ADR-001 반영됨)
- [ ] 🤖 홈 위젯 (iOS WidgetKit / Android Glance): 다음 차 N분·권장 출발 시각 표시 —
      계산은 Flutter/서버, 위젯은 표시만 (CLAUDE.md 데이터 계약 절)
- [ ] 🤖 iOS Live Activity: 출근 시간대 잠금화면 실시간 카운트다운 (FR-503 — 미니앱에선 불가였던 것)

### 3단계 — 자체 푸시 (FCM/APNs)
- [ ] 🤖 토스 스마트발송 제약(글자수·문구 규칙·발송 코드 단위 동의) 없는 리치 알림.
      아래 "동의 연속 요청 금지" 규칙 준수 + 전달 실패 관측 가능하게
- [ ] 🧑 Firebase 프로젝트/APNs 키 발급 — 프로젝트·iOS/Android 앱 등록 + firebase_core 초기화는 2026-09-09 완료(프로젝트 `flutter-with-firebase-39515`). Apple Developer 가입 완료 확인(2026-09-10, 팀 ZSF98GN32F — 실기기 릴리즈 서명 성공). 남은 것: APNs 키를 Firebase 콘솔에 등록. 토큰 등록 서버 계약 초안은 API.md §4-1

### 4단계 — 수익화 (리텐션 지표 확인 후 착수)
- [ ] 광고: 라이브 뷰 하단 **네이티브 배너까지만** — 전면 광고 금지(출근 직전 이탈 = NFR-01급).
      광고 SDK는 Platform View 허용 목록에 포함(ADR-001)
- [ ] 프리미엄 병행: 무광고 + 경로 확장 + 위젯 커스텀 묶음 — 광고 단독 수익화는 세션이 짧아 한계

## 차기 기획 — 환승 방면 안내 · 이동 중 브리핑 (오너 결정 2026-09-10, PM 세션)

착수는 1~2단계(패리티·시스템 표면) 이후 — 전략 순서(유저 기반 먼저, PRD §8)를 지킨다.

### 환승 방면 안내 — 온보딩 경로 추천 + 저장 경로 다구간화

O/D 경로 탐색 자체를 제품화하지 않는다(명세서 §1.4 "지도 앱의 영역" 유지). 경로 추천은
**통근 설정 생성 도우미 1회용**이고, 매일의 가치는 저장 경로의 환승 방면이 라이브 뷰·푸시에
녹아드는 것으로 전달한다.

- [ ] 🧑 경로 추천 외부 API 선정 — ODsay 등 상용 API 재검토 승인 필요
      (PRD §5.2 "보류" 결정의 예외 승인 + 무료 티어 호출 한도 확인. 검색은 유저당 사실상 1회)
- [ ] 🤖 온보딩 "출발지·목적지로 경로 추천받기": 추천 경로 선택 시 정류장·노선·환승 구간·
      각 환승의 방면이 통근 설정으로 저장 — 온보딩 완료율(명세서 §6 ≥60%) 개선 목적.
      서버 API 신설 — API.md에 계약 추가 후 진행
- [ ] 🤖 통근 설정 도메인 "첫 탑승 구간" → 다구간(환승 포함) 확장 + 라이브 뷰·푸시에 방면
      문구 노출 ("당산역 2호선 성수 방면, 급행 4분 후" — FR-203 방면 데이터 활용).
      서버·앱 양측 개정이 큰 작업이라 1단계 패리티 완료가 선행 조건

### 이동 중 뉴스 브리핑 — "탔어요" 연동 릴스

타깃 시간대는 출발 판단 순간이 아니라 **탑승 후 이동 시간**. 진입점은 FR-601 "탔어요"
원탭 — 브리핑이 피드백을 누를 이유가 되어 North Star 측정(응답률)도 같이 오르는 구조.

- [ ] 🤖 v1 (헤드라인 릴스): "탔어요" 탭 직후 유저 지정 분야의 헤드라인 카드 스와이프
      (제목+발췌+원문 링크, LLM 요약 없음 — 저작권·비용 회피, 뉴스 소스 약관 내 사용).
      측정: 탑승 후 열람률·체류 시간·피드백 응답률 변화
- [ ] 🤖 v1.5 (BYOK 옵트인 "내 AI 연결"): 유저의 OpenAI/Anthropic/Gemini API 키를 기기
      secure storage에만 저장, 클라이언트가 직접 호출해 요약 릴스 생성 — 앱은 프롬프트만
      제공, 키·기사 원문은 서버로 보내지 않는다(NFR-05 준용). 소비자 구독 계정 연동은
      공식 경로 부재로 제외 — API 키 방식 한정, 코어 경로가 아닌 파워유저 레이어
- [ ] 🤖 명세서 §9에 결정 기록 추가: "탑승 후 이동 시간을 제품 범위에 포함" — 알림 앱
      포지셔닝의 확장이므로 기록 필수
- [ ] 🧑 v2 (수익화 연계, 4단계에서 결정): 프리미엄에 앱 부담 LLM 요약 브리핑 포함(구독으로
      비용 상쇄) + 릴스 내 네이티브 광고 — v1 열람률·리텐션 지표 확인 후 착수

## 신규 기획 — 하차 알림 (오너 결정 2026-09-14, PM 세션)

출근 잡을 "언제 나가야"에서 **"언제 내려야"까지** 확장한다. 유저가 출발지·환승·목적지를
직접 입력한 **여정(Journey)**을 만들고, 탑승 시 직접 시작하면 **서버가 공공 API로 차량을
추적**해 하차·환승 타이밍을 푸시로 알린다. 경로 "탐색"은 여전히 비범위(명세서 §1.4) —
유저가 스스로 입력한 여정의 "추적·안내"만 다룬다. 지하철은 GPS 불가 구간이라 위치는
클라이언트가 아니라 서버(열차 위치 API)가 안다 — NFR-04/05(상시 추적 금지) 저촉 없음,
"모든 타이밍 계산·발송은 서버 책임"(PRD §3)과 정합. UI 자리는 "새로운 기능" 탭.

확정 결정 (2026-09-14 오너 인터뷰):
① 판정 = 서버 차량 추적(클라 GPS 아님) ② 여정 = 유저 직접 입력(출발지·환승 구간·목적지)
③ 시작 = 유저 수동 시작(자동 감지 없음 — 오판 리스크 회피, NFR-01)
④ 표면 = FCM 푸시 + iOS Live Activity ⑤ 재사용 = 히스토리 재선택 + 저장 + 요일 반복
⑥ v1 = 지하철 먼저 (열차 위치 API로 정거장 단위 추적 용이), 버스는 v1.5

- [x] 🤖 (2026-09-14 완료 — §3.7 FR-701~707 신설) 명세서 §9 결정 기록 + 요구사항 ID 부여: "하차 알림 — 탑승 후 구간(하차·환승)을
      제품 범위에 포함, 경로 탐색은 계속 비범위" (§1.4와의 경계 명문화. 이동 중 브리핑의
      "탑승 후 시간" 확장과 같은 축)
- [x] 🤖 (2026-09-14 완료 — API.md §9, 서버 1f12977) 여정(Journey) 도메인·서버 계약: 구간 리스트(탑승 역 → 하차/환승 역, 노선·방면) +
      목적지, 저장 여정 CRUD·이용 히스토리·요일 반복 설정. 통근 경로(§1-2)와 별개 모델 —
      **API.md에 계약 추가 후 진행**
- [x] 🤖 (2026-09-14 완료 — 서버 9eb9e5a TripTrackingScheduler, 배포·실측 검증 잔여) 서버 추적 엔진 (v1 지하철): 트립 시작 API → 대상 열차 식별·정거장 단위 추적
      (서울열린데이터광장 실시간 열차 위치) → "N정거장 전"·"다음 역에서 내리세요" 푸시 →
      환승 구간이면 다음 구간 안내 → 종료. 트립당 이벤트별 발송 1회(FR-403 정신 준용,
      2026-09-10 사이클 폭주 사고 재발 방지), 폴링은 노선·열차 단위 캐시로 NFR-08 준수 —
      **API.md에 계약 추가 후 진행**
- [x] 🤖 (2026-09-14 완료 — develop 90e7244~466fdab) 클라 "새로운 기능" 탭 → 하차 알림 화면: 여정 만들기(정류장/역 검색 §5 재사용) ·
      저장 목록·히스토리에서 원탭 재시작 · 진행 중 화면(남은 정거장 카운트다운, 20초 폴링
      FR-204 준용) · 메인 대시보드에 진행 중 여정 요약 카드. 탭 이름 "새로운 기능" →
      기능명(가칭 "하차 알림")으로 교체
- [x] 🤖 (2026-09-14 완료 — ios/WidgetExtension + catchmyride/live_activity 브리지, 서버 푸시 갱신은 후속) iOS Live Activity 연동: 진행 중 여정의 잠금화면 카운트다운("○○역까지 3정거장") —
      2단계(시스템 표면) Live Activity 기반 작업과 합류. 위젯은 표시만, 갱신 값은 서버
      푸시/앱이 공급 (ADR-001·CLAUDE.md 데이터 계약 절)
- [ ] 🤖 버스 확장 (v1.5): 차량(vehId) 단위 추적 — 탑승 차량 특정 UX(시작 시점 도착 임박
      차량 추정)와 함께 설계. 지하철 v1 지표(시작→하차 완주율) 확인 후 착수

## 2026-09-09 — 미니앱에서 확정된 요구사항 (동일 반영 필수)

미니앱 운영 중 실측으로 잡힌 문제들이라, 플러터판은 처음부터 아래 규칙대로 구현한다.

- [x] 🤖 **위치 권한 요청을 측위 타임아웃 밖으로 분리** (온보딩 출발지 등록) — 2026-09-09 반영, `lib/platform/location.dart`
  - 권한 확인 → (필요 시) 권한 다이얼로그 → 허용된 뒤에만 측위 시작. 유저가 동의창을 읽는 시간에는 시간 제한을 두지 않는다.
  - 미니앱 사고: 권한 요청까지 12초 측위 타임아웃에 포함시켜 **최초 사용자의 첫 시도가 무조건 실패**했다.
  - 측위 자체는 고정밀을 3초까지만 기다리고 늦으면 저정밀과 경쟁, 전체 12초 상한 + 일시 오류 1회 재시도 (권한 거부·타임아웃은 재시도 금지). geolocator/permission_handler로 구현 시 같은 함정 주의.
- [x] 🤖 **GPS로 출발지를 등록하면 "어떤 주소로 잡혔는지" 문구로 확인시킨다** — 2026-09-09 반영, 온보딩 출발지 단계(현재 mock API — 실서버 교체 시 §7-1 그대로 동작)
  - `GET /api/v1/reverse-geocode?latitude=&longitude=` (API.md §7-1, 서버 v0.9.0 배포됨) 호출 → "○○로 12 기준으로 등록됐어요". 도로명 우선, 둘 다 비면 일반 안내 문구로 강등.
  - 표시 전용 — 역지오코딩이 실패(503 등)해도 등록 흐름은 계속된다.
- [x] 🤖 **도착 목록의 회색 비활성은 "시간 정보 없음"에만 쓴다** (라이브 뷰) — 2026-09-09 반영, 위젯 테스트 포함(`test/ui/live_view_screen_test.dart`)
  - 못 타는 차(도보 시간 대비 너무 빨리 도착)를 흐리게 하면 유저가 **이미 지나간 차로 오해**한다.
  - 시간 정보가 있으면 노선명은 활성 색 유지, 남은 시간을 "곧 도착" 느낌의 주황 계열로 표시 (탑승 가능 = 파랑과 구분). 상태 문구("다음 차를 노리세요" 등)는 그대로 — 색상 외 수단(NFR-06).
- [ ] 🤖 **알림 권한·수신 동의 요청을 연속으로 띄우지 않는다** (온보딩 알림 설정) — 푸시 채널(FCM/APNs) 구현 시 반영. 설계 규칙은 연동 지점(`lib/ui/onboarding_page.dart` 저장 완료 처리)에 주석으로 기록해 둠
  - 미니앱 사고: 동의 바텀시트를 연속 요청하니 네이티브가 두 번째를 삼켜 **리마인드만 미동의로 남아 전량 미전달**됐다(발송은 SUCCESS로 위장). 다이얼로그 사이 간격 + 실패 시 1회 재시도, 결과를 로깅해 조용한 실패를 남기지 않는다.
  - 스토어판 푸시 채널(FCM/APNs) 설계 시에도 같은 원칙: 동의/권한 상태를 단계별로 확인하고, "성공 응답 ≠ 실제 전달"을 전제로 전달 실패를 관측 가능하게.
