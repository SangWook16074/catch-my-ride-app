# TODO — 플러터 스토어판 (2026-09-09 시작)

> 🧑 = 오너만 할 수 있는 액션 / 🤖 = 에이전트 작업
> 기능 명세: [../PRD.md](../PRD.md) · 서버 계약: [../API.md](../API.md) · 실행 규칙: [CLAUDE.md](CLAUDE.md)
> 미니앱(catch-my-ride-appintoss)에서 검증된 요구사항을 이식한다 — 코드 번역이 아니라 규칙 이식.

## 2026-09-09 — 미니앱에서 확정된 요구사항 (동일 반영 필수)

미니앱 운영 중 실측으로 잡힌 문제들이라, 플러터판은 처음부터 아래 규칙대로 구현한다.

- [ ] 🤖 **위치 권한 요청을 측위 타임아웃 밖으로 분리** (온보딩 출발지 등록)
  - 권한 확인 → (필요 시) 권한 다이얼로그 → 허용된 뒤에만 측위 시작. 유저가 동의창을 읽는 시간에는 시간 제한을 두지 않는다.
  - 미니앱 사고: 권한 요청까지 12초 측위 타임아웃에 포함시켜 **최초 사용자의 첫 시도가 무조건 실패**했다.
  - 측위 자체는 고정밀을 3초까지만 기다리고 늦으면 저정밀과 경쟁, 전체 12초 상한 + 일시 오류 1회 재시도 (권한 거부·타임아웃은 재시도 금지). geolocator/permission_handler로 구현 시 같은 함정 주의.
- [ ] 🤖 **GPS로 출발지를 등록하면 "어떤 주소로 잡혔는지" 문구로 확인시킨다**
  - `GET /api/v1/reverse-geocode?latitude=&longitude=` (API.md §7-1, 서버 v0.9.0 배포됨) 호출 → "○○로 12 기준으로 등록됐어요". 도로명 우선, 둘 다 비면 일반 안내 문구로 강등.
  - 표시 전용 — 역지오코딩이 실패(503 등)해도 등록 흐름은 계속된다.
- [ ] 🤖 **도착 목록의 회색 비활성은 "시간 정보 없음"에만 쓴다** (라이브 뷰)
  - 못 타는 차(도보 시간 대비 너무 빨리 도착)를 흐리게 하면 유저가 **이미 지나간 차로 오해**한다.
  - 시간 정보가 있으면 노선명은 활성 색 유지, 남은 시간을 "곧 도착" 느낌의 주황 계열로 표시 (탑승 가능 = 파랑과 구분). 상태 문구("다음 차를 노리세요" 등)는 그대로 — 색상 외 수단(NFR-06).
- [ ] 🤖 **알림 권한·수신 동의 요청을 연속으로 띄우지 않는다** (온보딩 알림 설정)
  - 미니앱 사고: 동의 바텀시트를 연속 요청하니 네이티브가 두 번째를 삼켜 **리마인드만 미동의로 남아 전량 미전달**됐다(발송은 SUCCESS로 위장). 다이얼로그 사이 간격 + 실패 시 1회 재시도, 결과를 로깅해 조용한 실패를 남기지 않는다.
  - 스토어판 푸시 채널(FCM/APNs) 설계 시에도 같은 원칙: 동의/권한 상태를 단계별로 확인하고, "성공 응답 ≠ 실제 전달"을 전제로 전달 실패를 관측 가능하게.
